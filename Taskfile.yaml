version: 3

silent: true

tasks:
  default:
    desc: "Show available tasks"
    cmds:
      - task --list-all

  pre-commit:
    desc: "Run generators, formatters, linters, tests"
    cmds:
      - task: generate
      - task: format
      - task: lint
      - task: test

  run:local:
    desc: "Run service locally (host binary, emulator via localhost)"
    deps: [ firestore:up ]
    env:
      # Host process reaches OrbStack's forwarded emulator port directly.
      FIRESTORE_EMULATOR_HOST: localhost:8085
    cmds:
      - go run -tags local ./cmd/telegram-butler/

  run:docker:
    desc: "Run service locally in Docker (emulator via host.docker.internal)"
    deps: [ firestore:up ]
    env:
      # Inside the container, host.docker.internal resolves to the host machine.
      # `-e FIRESTORE_EMULATOR_HOST` forwards the shell value into the container.
      FIRESTORE_EMULATOR_HOST: host.docker.internal:8085
    cmds:
      - docker build -f Dockerfile.local -t telegram-butler:local .
      - docker run --rm --env-file .env -e FIRESTORE_EMULATOR_HOST -p 8080:8080 telegram-butler:local

  run:local:prod:
    desc: "Run service locally against REAL Firestore in PROJECT_ID. Requires `gcloud auth application-default login` first. NO emulator dep."
    env:
      # Empty value overrides whatever .env sets — godotenv won't re-set an existing var.
      # SDK treats empty FIRESTORE_EMULATOR_HOST the same as unset → uses real GCP via ADC.
      FIRESTORE_EMULATOR_HOST: ""
    cmds:
      - echo "⚠️  Real Firestore writes will land in PROJECT_ID/report_votes — use with care."
      - go run -tags local ./cmd/telegram-butler/

  firestore:up:
    desc: "Start Firestore emulator container (no-op if already running)"
    cmds:
      - |
        if [ -n "$(docker ps -q -f name=^fs-emulator$)" ]; then
          echo "Firestore emulator already running on :8085"
        else
          echo "Starting Firestore emulator on :8085..."
          docker run --rm -d --name fs-emulator -p 8085:8080 mtlynch/firestore-emulator:latest
          # Boot wait — emulator takes ~2-3s to print "Dev App Server is now running"
          sleep 3
        fi

  firestore:down:
    desc: "Stop Firestore emulator container"
    cmds:
      - |
        if [ -n "$(docker ps -q -f name=^fs-emulator$)" ]; then
          docker stop fs-emulator
        else
          echo "Firestore emulator not running"
        fi

  format:
    desc: "Run formatters"
    deps: [ install:gofumpt, install:gci ]
    aliases: [ fmt ]
    cmds:
      - echo "Running formatters..."
      - go mod tidy
      - gofumpt -l -w .
      - |
        gci write \
        --skip-generated \
        --section standard \
        --section default \
        --section "prefix(github.com/GolangUA/telegram-butler)" \
        $(go list -f "{{`{{.Dir}}`}}" ./...)
    sources:
      - ./**/*.go
      - go.mod
      - go.sum

  lint:
    desc: "Run golangci-lint"
    deps: [ install:lint ]
    cmds:
      - echo "Running linters..."
      - golangci-lint run
    sources:
      - .golangci.yaml
      - ./**/*.go
      - go.mod
      - go.sum

  lint:list:
    desc: "Run golangci-lint linters (print enabled & disabled linters)"
    deps: [ install:lint ]
    cmds:
      - golangci-lint linters

  test:
    desc: "Run tests"
    cmds:
      - echo "Running tests..."
      - mkdir -p bin
      - go test -coverprofile bin/cover.out ./...
    sources:
      - ./**/*.go
      - go.mod
      - go.sum
    generates:
      - bin/cover.out

  test:cover:
    desc: "Run tests & show coverage"
    deps:
      - test
    cmds:
      - go tool cover -func bin/cover.out

  test:race:
    desc: "Run tests with a race flag"
    cmds:
      - go test -race -count=8 -failfast ./...

  generate:
    desc: "Generate (used for mock generation)"
    deps: [ install:mock ]
    cmds:
      - echo "Running generators..."
      - go generate ./...
    sources:
      - ./**/*.go
      - go.mod
      - go.sum

  install:
    desc: "Install all tools"
    deps:
      - install:lint
      - install:gofumpt
      - install:gci
      - install:mock

  install:lint:
    desc: "Install golangci-lint"
    cmds:
      - curl -sSfL https://golangci-lint.run/install.sh | sh -s -- -b $(go env GOPATH)/bin v2.11.3
    status:
      - command -v golangci-lint

  install:gofumpt:
    desc: "Install gofumpt"
    cmds:
      - go install mvdan.cc/gofumpt@latest
    status:
      - command -v gofumpt

  install:gci:
    desc: "Install gci"
    cmds:
      - go install github.com/daixiang0/gci@latest
    status:
      - command -v gci

  install:mock:
    desc: "Install mockgen"
    cmds:
      - go install go.uber.org/mock/mockgen@latest
    status:
      - command -v mockgen
